import { TeamSizeFilterPipe } from './team-size.pipe';

describe('TeamSizeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamSizeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
